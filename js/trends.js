const Trends={render(){const s=App.records.filter(r=>r.type==="sleep"),h=App.records.filter(r=>r.type==="health"),n=App.records.filter(r=>r.type==="nutrition"),avg=(a,f)=>a.length?(a.reduce((x,r)=>x+f(r),0)/a.length).toFixed(1):"--",latest=[...h].sort((a,b)=>b.date.localeCompare(a.date))[0]?.weight||"--";trendStats.innerHTML=[["Avg sleep",avg(s,r=>Utils.hours(r.start,r.end))+" hr"],["Latest weight",latest+" lb"],["Avg protein",avg(n,r=>r.protein||0)+" g"],["Entries",App.records.length]].map(x=>`<div class="stat"><strong>${x[1]}</strong><small>${x[0]}</small></div>`).join("");weeklyAverages.innerHTML=`<div class="history-row"><span>Sleep quality</span><strong>${avg(s,r=>r.quality||0)}</strong></div><div class="history-row"><span>Water</span><strong>${avg(n,r=>r.water||0)} oz</strong></div>`;this.drawLine("readinessChart",s.sort((a,b)=>a.date.localeCompare(b.date)).slice(-14).map(r=>({label:Utils.formatDate(r.date),value:Math.min(100,50+Utils.hours(r.start,r.end)*5+(r.quality||0))})))},drawLine(id,pts){const c=document.getElementById(id);if(!c)return;const ctx=c.getContext("2d"),w=c.clientWidth||320,h=Number(c.getAttribute("height"))||180,d=devicePixelRatio||1;c.width=w*d;c.height=h*d;ctx.scale(d,d);ctx.clearRect(0,0,w,h);if(pts.length<2){ctx.fillStyle="#9bacbb";ctx.font="14px sans-serif";ctx.fillText("Add more entries to see a trend.",18,40);return}const vals=pts.map(p=>+p.value),min=Math.min(...vals),max=Math.max(...vals),range=max-min||1,p=28;ctx.strokeStyle="#243646";for(let i=0;i<4;i++){const y=p+i*(h-2*p)/3;ctx.beginPath();ctx.moveTo(p,y);ctx.lineTo(w-p,y);ctx.stroke()}ctx.strokeStyle="#62b5ff";ctx.lineWidth=3;ctx.beginPath();pts.forEach((q,i)=>{const x=p+i*(w-2*p)/(pts.length-1),y=h-p-(q.value-min)/range*(h-2*p);i?ctx.lineTo(x,y):ctx.moveTo(x,y)});ctx.stroke()}};