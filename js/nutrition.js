const Nutrition={async save(){await Database.put({id:Utils.id("nutrition"),type:"nutrition",date:Utils.today(),protein:Number(protein.value||0),water:Number(water.value||0),caffeine:Number(caffeineMg.value||0),cutoff:caffeineCutoff.value});await App.refresh();Navigation.showPage("today")},renderHistory(){const rows=App.records.filter(r=>r.type==="nutrition").sort((a,b)=>b.date.localeCompare(a.date)).slice(0,8);nutritionHistory.innerHTML=rows.length?rows.map(r=>`<div class="history-row"><span>${Utils.formatDate(r.date)} · ${r.protein||0}g protein · ${r.water||0} oz · ${r.caffeine||0}mg caffeine</span></div>`).join(""):`<p class="muted">No entries yet.</p>`}};