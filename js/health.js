const Health={async save(){await Database.put({id:Utils.id("health"),type:"health",date:Utils.today(),weight:Number(weight.value||0),bodyFat:Number(bodyFat.value||0),waist:Number(waist.value||0),energy:Number(energy.value||0),mood:Number(mood.value||0)});await App.refresh();Navigation.showPage("today")},renderHistory(){const rows=App.records.filter(r=>r.type==="health").sort((a,b)=>b.date.localeCompare(a.date)).slice(0,8);healthHistory.innerHTML=rows.length?rows.map(r=>`<div class="history-row"><span>${Utils.formatDate(r.date)} · ${r.weight||"-"} lb · energy ${r.energy||"-"} · mood ${r.mood||"-"}</span></div>`).join(""):`<p class="muted">No entries yet.</p>`},drawChart(){Trends.drawLine("weightChart",App.records.filter(r=>r.type==="health"&&r.weight).sort((a,b)=>a.date.localeCompare(b.date)).slice(-20).map(r=>({label:Utils.formatDate(r.date),value:r.weight})))}};