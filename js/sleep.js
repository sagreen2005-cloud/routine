const Sleep={async save(){await Database.put({id:Utils.id("sleep"),type:"sleep",date:Utils.today(),start:sleepStart.value,end:sleepEnd.value,quality:Number(sleepQuality.value||0),awakenings:Number(awakenings.value||0),magnesium:magnesiumTaken.checked});await App.refresh();Navigation.showPage("today")},renderHistory(){const rows=App.records.filter(r=>r.type==="sleep").sort((a,b)=>b.date.localeCompare(a.date)).slice(0,8);sleepHistory.innerHTML=rows.length?rows.map(r=>`<div class="history-row"><span>${Utils.formatDate(r.date)} · ${Utils.hours(r.start,r.end).toFixed(1)} hr · quality ${r.quality||"-"}/10</span></div>`).join(""):`<p class="muted">No entries yet.</p>`}};