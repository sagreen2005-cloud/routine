const Navigation={showPage(page){document.querySelectorAll('.page').forEach(p=>p.classList.remove('active'));const el=document.getElementById(page+'Page');if(!el)return;el.classList.add('active');pageTitle.textContent=Config.pageTitles[page]||'Routine';document.querySelectorAll('.bottom-nav button').forEach(b=>b.classList.toggle('active',b.dataset.page===page));moreMenu.classList.add('hidden');window.scrollTo(0,0);if(page==='health')Health.drawChart();if(page==='trends')Trends.render();if(page==='fitness')Workouts.render()},toggleMoreMenu(){moreMenu.classList.toggle('hidden')}};
