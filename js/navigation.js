const Navigation={showPage(page){document.querySelectorAll(".page").forEach(p=>p.classList.remove("active"));document.getElementById(page+"Page").classList.add("active");pageTitle.textContent=Config.pageTitles[page];document.querySelectorAll(".bottom-nav button").forEach(b=>b.classList.toggle("active",b.dataset.page===page));window.scrollTo(0,0);if(page==="health")Health.drawChart();if(page==="trends")Trends.render()},toggleMoreMenu(){moreMenu.classList.toggle("hidden")}};