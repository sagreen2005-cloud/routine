const Workouts={plan(){const custom=JSON.parse(localStorage.getItem("customWorkoutPlan")||"null");return custom?.[new Date().getDay()]||Config.workouts[new Date().getDay()]},render(){const p=this.plan();workoutDayBadge.textContent=p.name;workoutOfDay.innerHTML=p.items.map(x=>`<div class="history-row"><span>${x}</span></div>`).join("");workoutComplete.checked=!!Utils.record("workout")?.complete},async save(){await Database.put({id:Utils.id("workout"),type:"workout",date:Utils.today(),complete:workoutComplete.checked});await App.refresh();Navigation.showPage("today")},async importFile(){const f=workoutFile.files[0];if(!f)return alert("Choose a file.");const text=await f.text();let plan;try{plan=JSON.parse(text)}catch{plan={2:{name:"Imported Workout",items:text.split(/\r?\n/).map(x=>x.trim()).filter(Boolean)}}}localStorage.setItem("customWorkoutPlan",JSON.stringify(plan));workoutImportStatus.textContent="Workout plan imported.";this.render()}};